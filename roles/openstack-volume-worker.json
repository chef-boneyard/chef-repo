{
  "name": "openstack-volume-worker",
  "default_attributes": {
  },
  "json_class": "Chef::Role",
  "env_run_lists": {
  },
  "run_list": [
    "role[base]",
    "role[openstack-base]",
    "recipe[cinder::volume]"
  ],
  "description": "A role that exposes the OpenStack Volume service. This role assumes that block device setup has already been done on the node.",
  "chef_type": "role",
  "override_attributes": {
  }
}
